#pragma once
#include <string>
// pre: nothing
// post: returns the user's choice
char menu();

// pre: string
// post: returns whether or not the string is all digits or not 
bool isDigits(string s);